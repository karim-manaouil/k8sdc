apiVersion: apps/v1
kind: Deployment
metadata:
    name: "Defacto Deployment"
spec:
    replicas: 1
    selector:
        matchLabels:
            app: test
    template:
        metadata:
            labels:
                app: test
        spec:
            containers:
                - image: gcr.io/google-containers/pause-amd64
